NONE_HELDOUT_CONTAMINATION_REQUIRES_NEW_PREREGISTRATION

Do not execute any runner. The current held-out toy seed universe was exposed by
`tests/psvr_rollout_toy/test_unfrozen_invariants.py` during the required focused
test audit, so it cannot be used for confirmation. A future, separately authorized
result-blind amendment must first address the visible-history conditional-kernel
defect and then create/hash-bind a new, previously unseen held-out universe.
