coarse_5s_clip_grid:
  clip_id: clip_id
  video_id: video_id
  source_video_path: source_video_path
  start_time: start_time
  end_time: end_time
  duration: duration
proxy_features_5s:
  clip_id: clip_id
  motion_energy_mean: motion_energy_mean
  vehicle_count_mean: vehicle_count_mean
  vehicle_count_max: vehicle_count_max
  bbox_area_sum_mean: bbox_area_sum_mean
  center_roi_vehicle_count_mean: center_roi_vehicle_count_mean
  bottom_roi_vehicle_count_mean: bottom_roi_vehicle_count_mean
center10_anchor_grid:
  anchor_id: anchor_id
  anchor_time: anchor_time
  start_time: start_time
  end_time: end_time
  source_video_path: source_video_path
center10_proxy_features:
  anchor_id: anchor_id
  yolo_vehicle_max: yolo_vehicle_max
  yolo_vehicle_mean: yolo_vehicle_mean
  motion_energy_max: motion_energy_max
  score_fusion_yolo_motion: score_fusion_yolo_motion
