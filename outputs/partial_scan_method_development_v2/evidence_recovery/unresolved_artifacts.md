# Unresolved source artifacts

- `SOURCE_ARTIFACT_MISSING`: separately versioned V1 M5 implementation/configuration artifact.
- `SOURCE_ARTIFACT_MISSING`: V1 run commit and environment identity sufficient for an independent source-level rerun.
- `UNATTESTED_LOCAL_METADATA`: repository commit identity; this worktree rejects local git inspection because of ownership protection.

These gaps do not block the new-video confirmation pilot. They do prevent a
fully reproducible V1 provenance claim, and they do not affect the standing
V1 lookahead invalidation.
