# PSVR H-ROLLOUT1A-R2 final report

`INPUT_BLOCK_STATE = HELDOUT_CONTAMINATION_DETECTED / R1_BLOCKED_SCIENTIFIC_SPEC_CONFLICT`.

`AMENDMENT_ID = D3D4_VISIBLE_HISTORY_KERNEL_R2` and
`RESULTS_VISIBLE_BEFORE_AMENDMENT = false`.

`VISIBLE_HISTORY_STATE = COMPLETE`; it contains only legal action/observation
history and deterministic aggregates. `OBSERVATION_LIKELIHOOD_STATE = COMPLETE`:
actions are interventions and likelihood is an exact finite deterministic
indicator. `EXACT_POSTERIOR_STATE = COMPLETE`: 288 finite worlds are exactly
enumerated, with independent oracle and incremental-update verification.
`CONDITIONAL_KERNEL_STATE = COMPLETE`: M1 uses one posterior, one full horizon,
one closed-loop pi0 continuation and no actual-future access.
`RNG_SEMANTICS_STATE = COMPLETE` with independent HKDF/SplitMix namespaces.

`D3_R2_AMENDMENT = FROZEN`; `H_ROLLOUT1A_R2_PREREGISTRATION = FROZEN`;
`SPLIT_ISOLATION = COMPLETE`; and
`CONTAMINATED_UNIVERSE_STATUS = PERMANENTLY_NON_CONFIRMATORY`.

`DEVELOPMENT_SMOKE = PASS`; `CODE_FREEZE = COMPLETE`; and
`ADVERSARIAL_REVIEW = CLEAR_AFTER_R2_REPAIR_01`. The freeze manifest has 55
entries and recomputed with zero hash mismatches.

`CONFIRMATORY_SEEDS_GENERATED = false`; `CONFIRMATORY_RESULTS_EXIST = false`;
`PHYSICAL_CALLS_STARTED = 0`; `GPU_JOBS_STARTED = 0`; and
`REAL_HELDOUT_OPENED = false`.

`R2_FINAL_STATE = FROZEN_PRE_CONFIRMATORY`. There are no remaining R2 blockers.
The next research stage is the separately authorized one-shot confirmatory
execution. The sole frozen command is in `NEXT_CONFIRMATORY_COMMAND.md`; it was
not executed in this work.
