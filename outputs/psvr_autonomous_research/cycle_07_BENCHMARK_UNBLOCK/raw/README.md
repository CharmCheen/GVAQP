# Raw audit scope

No media, held-out content, proxy output, or oracle output is copied here. Path-level metadata and 606 hashes computed from approved development candidates are retained in the authoritative `benchmark_unblock/VIDEO_CANDIDATES.csv`; the held-out identity/hash row is explicitly marked as metadata copied from a pre-existing manifest. Historical lineage evidence is referenced in the decision artifacts.
