# Exact commands

```bash
cd /qiuyeqing/llama_prl/G-ARC
python scripts/audit_psvr_dev_inputs.py
python scripts/verify_psvr_dev_input_audit.py
```

The same audit command is the exact resume command after new files are placed under `data/realcam/psvr_dev_inputs/`.
