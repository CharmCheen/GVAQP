# Next exact command

H-FACT1 is complete and this bounded cycle must stop. No next experiment command is authorized in this cycle.

For a later, separately authorized bounded cycle, the first command should be a preregistration-only command (to be implemented then) for the scan-priority component ablation. Do not run multi-video validation, parameter tuning, or held-out evaluation now.
