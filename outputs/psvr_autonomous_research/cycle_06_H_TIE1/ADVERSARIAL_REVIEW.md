# H-TIE1 blocking review

The block is not inferred from a failed method. It is caused by missing inference units required to test the hypothesis. Treating `realcartest_5k` as independent would be pseudoreplication because the existing manifest identifies it as a derivative clip. Treating physical repeats or disjoint segments of `long_video_dataset3` as separate source videos would likewise violate the task definition. `test.mov` does not repair the design: it is only 43 seconds and its five existing oracle-label rows all failed to produce a stable reference.

Generating a second query on the existing one source would still leave the independent-video requirement unmet. Consequently no local physical experiment can discriminate H-TIE1 without new source assets and references.
