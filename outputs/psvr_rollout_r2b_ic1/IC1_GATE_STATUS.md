# H1B development gate status

`H1B_DEVELOPMENT_GATE = BLOCKED_INDEPENDENT_VERIFIER`

This is a true execution-integrity blocker, not a negative scientific result.
No H1B confirmatory seed, runner, result, or scientific-grid artifact has been created.

## Current A4 recovery disposition

The A4 clarification now supplies a production transition implementation and
focused regressions, but the new blind reassessment proved that the purported
independent verifier accepts a forged A4 post-state and forged paired
continuation-return/Q values after affected commitments are recomputed. A4 is
therefore not frozen and IC1 development execution remains disabled. See
`outputs/psvr_rollout_r2b_a4/A4_INDEPENDENT_RED_TEAM_REASSESSMENT.md`.

## Historical root cause

The result-blind A1/A2 artifacts specify the six error targets, canonical identities, sign rules, and probability/duration transforms, but do not define a unique generative transition kernel for the deterministic R2 world model. Consequently, there is no authorized way to replace the invalid v9 post-hoc payoff offsets with the required generative simulations. The concrete proof is retained in `IC1_CANONICAL_SPEC_AMBIGUITY_PROOF.md`.

## Decisive evidence

A1 states that candidate yield, grouping, novelty, and materialization are probabilities and that all random draws use a canonical keyed rule. A2 adds target identities and draw indices. Neither frozen artifact supplies (1) the baseline probabilities in a sampled deterministic R2 world, (2) a probability-to-trajectory realization mapping, or (3) the state transition that results from a grouping draw. These choices materially change the approximate policy and cannot be inferred from outcomes.

## Competing explanation and disposition

One could select an arbitrary hash-to-uniform convention and a grouping merge/split rule. That would be a new model definition, not a repair of an existing executable definition. It is therefore rejected pending a new frozen, result-blind clarification.

## Repairs completed before the blocker

- Planning time is now charged to executor net D1, approximate continuation values, and exact evaluator-only values.
- Post-planning SCAN/CONFIRM legality is represented in the visible policy state.
- The exact sidecar is calculated after the selected action record is appended.
- The blind red-team’s complete original report is retained in `IC1_BLIND_RED_TEAM_REVIEW_V9.md`.

The remaining red-team failures (generative model, independent verifier, universe declaration, source closure, and resource reconciliation) remain unresolved and must not be papered over while the transition kernel is undefined.
