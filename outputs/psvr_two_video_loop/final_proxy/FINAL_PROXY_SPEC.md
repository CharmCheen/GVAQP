# Final two-video proxy

`SELECTED_PROXY_FAMILY = YOLOV8`

`SELECTED_PROXY_CONFIG = Y8`

The detector, weights, resolution, 5 FPS unit-local physical path, frozen ByteTrack parameters, query heads, causal percentile normalization, and fallback are bound by `FINAL_PROXY_CONFIG.json`.
