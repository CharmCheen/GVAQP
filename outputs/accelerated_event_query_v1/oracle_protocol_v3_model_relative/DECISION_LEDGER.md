# V3 Decision Ledger

| Cycle | Observation | Decision | Evidence | Next action |
|---|---|---|---|---|
| 01 OBSERVE | V2 failed strict parse at 31/32 and had semantic/grounding concerns. | Preserve V2 `REVISE_ORACLE_PROTOCOL`; no post-hoc repair. | V2 decision and `eef5050cd`. | Define a new authority boundary. |
| 02 HYPOTHESIZE | The dominant protocol failure is free-form event timing; V2 labels were deterministic. | Test label-authoritative semantics with K3 timing. | V2 raw/repeat hashes. | Build minimal schema/parser. |
| 03 DESIGN | Existing online K3 treats all nonnegative candidates as support. | Add a reference-only unit adapter; leave existing K3 unchanged. | `incremental_k3.py` audit. | Test gap/deadline semantics. |
| 04 IMPLEMENT/TEST | Parser, label store, adapter, fail-closed runner/analyzer, and tests exist. | Accept local evidence only. | 94 tests pass. | Freeze physical design. |
| 05 FREEZE | Eleven calls cover regressions, repeats, replica, fps, videos, and potential unknown. | Freeze 5/3/3, zero retry, 0.19-hour estimate. | Frozen manifests. | Seal and validate. |
| 06 AUDIT | First review found unauthenticated evidence-chain and same-process claims. | Reject old seal and repair before any compute. | Independent `NO-GO` for `84d352fb…`. | Bind sessions, ledger/raw/runtime, parsed outputs, and provenance. |
| 07 RESEAL | Repairs pass 94 tests; all model files and 251 frame occurrences revalidate; tensors/labels remain unobserved. | `INSUFFICIENT_EVIDENCE`. | Seal `bf35f7f3…`; CPU validation. | Repeat independent review, then request approval only on `GO`. |
| 08 REVIEW | Final reviewer found no remaining blocker for the exact reseal. | `GO` to request exact compute approval; not permission to execute. | Independent review for `bf35f7f3…` at `921fa8b9c`. | Stop and await user approval. |
| 09 APPROVE | User explicitly replied `批准` to the exact seal request. | Create the bound approval artifact only after revalidating all bindings and idle scheduled GPUs. | Approval SHA `f41aa836…`. | Execute three frozen shards once. |
| 10 EXECUTE | All 11 calls completed with 11/11 strict parse, 44 successful ledger events, and no failure/retry. | Accept physical evidence. | Raw, parsed, runtime summary, and evidence manifest. | Run frozen analyzer. |
| 11 AUDIT | Authentication, input binding, label reproducibility, class support, and K3 eventization all pass; diagnostics remain non-gating. | Candidate PASS. | Metrics `2003e29a…`; three diagnostic disagreements retained. | Run frozen finalizer. |
| 12 DECIDE | Finalizer recomputed analyzer output and authenticated parsed/evidence membership. | `V3_SCHEMA_DETERMINISM_PASS_FULL_GRID_APPROVAL_REQUIRED`. | Decision payload `4dc2292a…`. | Stop; prepare/review proposal only, with no full-grid execution. |
| 13 PROPOSE | The exact grid has 1,475 units; three final units are truncated and rejected by the current integral-interval sampler. | Propose 567/561/347 calls, about 16.097 A100 GPU-hours, and a source-anchored tail rule; do not expand inputs. | Frozen grid/video hashes and preflight runtime. | Seek independent proposal review. |
| 14 ADVERSARIAL AUDIT | Counts/cost/tail arithmetic passed, but the current controller API exposes caller-requested labels; reload, cross-shard failure, and partial-publication semantics were open. | `REVISE_FULL_GRID_PROPOSAL`. | Independent review of JSON `21bf20d5…`; preserved as `FULL_GRID_PROPOSAL_REVIEW_V1.md`. | Freeze capability, load, global-stop, and complete-only publication boundaries. |
| 15 REVISE/REVIEW | Revision 2 closes all four blockers without changing the 1,475-call model/protocol workload. | `GO_TO_PREPARE_FULL_GRID_PREREGISTRATION`; proposal-stage GO only. | Independent re-review of JSON `ffc47c8b…` and document `69be8d05…`. | Stop and request separate authority to implement/freeze/seal; no full-grid compute. |
| 16 OBSERVE | The existing exact sampler rejects three non-half-second tails; the current label-store controller view is not a capability boundary. | Implement separate full-grid input and hiding modules; preserve every preflight byte. | Source audit and proposal V1 rejection. | Test the smallest tail/hiding/fail-stop mechanisms before decoding. |
| 17 HYPOTHESIZE/DESIGN | A source-anchored tail grid, static video workers, global coordinator, and atomic release pointer should close the dominant 16-GPU-hour interpretation risks. | Freeze zero parse-failure tolerance and >=99% global/per-video determined coverage before outputs. | User requirements and unobserved preflight unknown/failure support. | Implement validators, runner, analyzer, finalizer, and injections. |
| 18 IMPLEMENT/TEST | Full-grid-only modules now enforce 12/2/6 tails, exact partitions, three loads/zero reload/retry, global stop, causal label reveal, and complete-only publication. | Accept local mechanics only; no claim about real decoded inputs yet. | 115 tests pass; no checkpoint/model generation. | Commit source, then decode all real inputs and run processor-only tail audit. |
| 19 FALSIFY/REVISE | First real decode stopped at 11/12 DALI tail frames: container end 5665.535333s exceeds the 169,962-frame video stream ending near 5665.381s. | `REVISE_FULL_GRID_PREREGISTRATION`; no seal. Preserve the 5.5s target but explicitly resolve ideal index 169965 to unique nearest available index 169961; reject any repeated padding. | ffprobe stream metadata, last-frame timestamps, zero package files, zero inference. | Test/recommit the revised boundary rule and restart the complete decode once. |
| 20 BUILD/VERIFY | Revised boundary decode produced 1,475 unique units, 30,932 frames, legal 12/2/6 tails, real processor-only tail PASS, 117 tests, complete mock dry-run, and full re-decode. | Freeze seal V1 for adversarial review only; not compute approval. | Seal `ffcfb934…`; bundle `f558e6fd…`; zero inference/output. | Try to falsify abrupt death, processed identity, label hiding, and bindings. |
| 21 ADVERSARIAL REJECT | Abrupt W0 death allowed W1 reservation; processed hashes lacked preregistered expectations; same-UID `0600` allowed guessed-path access. | `REVISE_FULL_GRID_PREREGISTRATION`; preserve and invalidate seal V1 for approval. | Independent review artifact `ec183b1e…`; reproduced coordinator and access failures. | Add supervisor, all-unit processor identities, and OS isolation. |
| 22 REVISE/TEST | Sole supervisor, operation leases, parent authority, pre-generation tensor comparison, root/UID isolation, and real setuid denial are implemented. | Accept only local mechanics; 120 tests are insufficient until all 1,475 tensors are frozen and independently reprocessed. | 120 tests pass; no checkpoint/model generation. | Commit source, rebuild full package, reprocess/redecode, reseal, dry-run, and re-review. |
| 23 FALSIFY/REVISE | Seal V2 froze and cross-process revalidated all 1,475 tensor identities, but the complete dry-run's partial fixture expected `INSUFFICIENT_EVIDENCE` while missing supervisor audit correctly mapped to the higher-priority runtime abort. | Preserve seal V2 and revise only the dry-run assertion; safety held because no formal release was produced. | Seal `f986ddc4…`; 9/9 hard injections PASS; `partial_nonpublication` assertion false; release pointer absent. | Recommit, rebuild/reseal, rerun complete dry-run, then independent review. |
| 24 REBUILD/REVIEW | Seal V3 reproduced all exact input identities and passed its complete mock and ten injections, but new-session workers could outlive a dead supervisor and a post-load/pre-reservation stall was unleased. | `REVISE_FULL_GRID_PREREGISTRATION`; preserve and invalidate seal V3. | Seal `f84a1861…`; bundle `8bcc7f32…`; independent review `56dcf8b7…`; zero model inference. | Add kernel parent-death enforcement and cover the full loaded-worker lifecycle. |
| 25 REVISE/TEST | Workers now install parent-death `SIGKILL`; reservation precedes decode; loaded-idle gaps are leased and GPU-accounted through explicit three-session closure. | Accept local mechanics only; exact rebuilt package must pass full mock/fault testing and independent review. | 123 tests pass, including real parent death and loaded-idle injection; expanded-budget use 0.0 A100 hours. | Commit, rebuild/reseal, run complete dry-run, and submit exact hashes for third review. |
| 26 SELF-CRITIQUE/REVISE | The first lifecycle repair excluded a session immediately on close, leaving a possible closed-but-live process tail. | Stop the empty CPU build; release model/tensors before close, monitor a live PID after close, and reserve the bounded exit tail. | 124 tests pass, including closed-session/live-worker expiry; no manifest, model load, inference, or GPU use occurred. | Commit the stronger lifecycle boundary, then perform one clean rebuild. |
| 27 REBUILD/REVIEW | Seal V4 reproduced all inputs and passed three independent full reprocesses, 124 tests, and eleven injections, but 1,475 durable-output gaps were only retrospectively charged and current foreign GPU occupancy was unauthenticated. | `REVISE_FULL_GRID_PREREGISTRATION`; preserve and invalidate seal V4. | Seal `48c71239…`; bundle `50bfcb9c…`; reviewer decision; real 68–99% busy-GPU evidence; zero formal calls. | Add prospective reusable residency reserve and GPU exclusivity authentication. |
| 28 REVISE/TEST | Calls now remain reserved through durable acceptance; eight-second per-worker emergency reserves persist through observed exit; busy/context/memory profiles fail before load. | Accept local mechanics only; rebuild/reseal and re-review exact cost transitions. | 129 tests pass; real occupied six-GPU authentication rejects; actual cost remains <= envelope in next-call denial test. | Commit, rebuild, dry-run, and submit fourth exact review bundle. |
| 29 SELF-CRITIQUE/REVISE | Runner GPU snapshots were recorded but not independently authenticated by the analyzer/finalizer evidence chain. | Interrupt the empty CPU build; bind initialization and every preload snapshot, UUID continuity, and idle/context policy into analyzer authentication. | 130 tests pass; tampered UUID/idle snapshots fail; no package file or inference was created. | Commit the end-to-end binding, then rebuild once. |
| 30 DRY-RUN REJECT | Seal V5 passed 130 tests, but the serial mock opened all three sessions before processing video-major records, creating artificial multi-minute idle for later shards and correctly tripping the eight-second reserve. | Reject seal V5; change only mock scheduling to one serialized static shard session at a time. | Seal `595a74dc…`; no dry-run audit, approval, formal output, model load, or GPU use. | Test/commit the fixture correction, rebuild, and rerun complete mock. |
| 31 ADVERSARIAL REJECT/REVISE | Seal V6 passed 130 tests and the complete mock, but reviewer injection exposed an uncharged complete-call coordinator seam; model-visible tail text falsely retained the nominal 10-second assertion; V2 history was not explicitly bound. | `REVISE_FULL_GRID_PREREGISTRATION`; preserve V6, move authoritative cost to a continuous coordinator clock, explicitly compose tail text, and bind unchanged V2 evidence. | Seal `d2f35022…`; bundle `102d7d25…`; 0.25s injection produced ~0.508 uncharged GPU-s; exact 2-frame tail prompt inspection; zero formal calls. | Run targeted counterexamples, commit, rebuild, independently reprocess all three shards, reseal/dry-run/re-review. |
| 32 RESOURCE-SCHEDULE REVISION | The reviewed simultaneous-start package remained blocked by foreign GPU occupancy; GPU 2/6 were repeatedly the only exact idle pair. User explicitly required starting one full video shard now and marking the rest pending. | Revise only execution scheduling to fixed staged activation; retain exact calls/inputs/schema, three loads, zero retry, global fail-stop, and complete-only release. | Real GPU telemetry; staged supervisor tests; 136 tests pass; zero formal calls. | Commit, rebuild/reseal, dry-run, independent review, then launch DALI on `(2,6)`. |

Revision mapping:

- schema/parse/label reproducibility: `REVISE_V3_SCHEMA`;
- authenticated input, processed-tensor, or session mismatch:
  `REVISE_V3_INPUT_BINDING`;
- K3 nondeterminism/authority leak: `REVISE_K3_EVENTIZATION`;
- incomplete/unauthenticated evidence: `INSUFFICIENT_EVIDENCE`.
