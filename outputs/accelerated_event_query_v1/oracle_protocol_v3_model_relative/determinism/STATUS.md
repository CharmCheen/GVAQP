# Determinism Status

`PASS`

All three same-process pairs and the cross-replica pair have exact authoritative
label, processed-input, and raw-response equality. Each shard has exactly one
authenticated execution session and the three shard sessions are distinct.
