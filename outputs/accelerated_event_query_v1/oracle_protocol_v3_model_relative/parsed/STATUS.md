# Parsed Output Status

`COMPLETE_AUTHENTICATED_11_OF_11`

The sealed analyzer wrote one hash-bound parsed artifact per call: 11 `ok`, 10
`not_relevant`, 1 `relevant`, 0 `unknown`, and 0 `parse_failure`. Unknown and
parse-failure preservation rules remain active even though neither occurred.
