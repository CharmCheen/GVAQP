# Mechanism Gate Final and Routing

## Final status

`algorithmic_mechanism_viability_gate_v1` is complete and sealed. Do not resume or rerun it.

```text
decision = IDEAL_SIGNAL_ONLY
settings = 149/149
policy_runs = 141750
physical_vlm_calls = 0
real_cheap_primitive_engineering_justified = false
```

## Interpretation

Event saturation is a real controlled mechanism when hypotheses are already aligned with latent events. It is not currently an operational algorithm because it fails to transfer to frozen public H1 representation.

Exploration is harmful in the controlled and event-aligned settings. Counterfactual materialization has effects near zero. The structure/barrier causal sweep is not valid because `merge_ambiguity` was not used and nominal levels were identical.

Therefore:

```text
P2 exploration                  → STOP
P3 counterfactual materializer  → STOP
real cheap primitive extraction → NOT JUSTIFIED
full AEH-AQP planner            → NOT CLAIMABLE
P1 saturation                  → one final representation-transfer test only
EventRelation + BB-EM/operator  → retained mainline
```

## Exact next gate

Run `public_event_cell_representation_gate_v1` using the frozen S2 action universe. Only one public R1 rule is permitted. Compare R0/R1 under P0/P1. Do not change candidate coverage or add features.

## Route after the next gate

### `PUBLIC_EVENT_CELL_SATURATION_GO`

Retain a minimal event-cell + saturation acquisition policy. Still discard exploration/counterfactual components. Next test it on multiple videos before making a planner claim.

### `PLANNER_ROUTE_REJECTED`

Permanently close BCM/saturation planning. Start the Barrier-Constrained Event Partition/Materialization operator task:

- formal optimization problem;
- exact or provably correct dynamic program;
- incremental maintenance;
- positive-anchor coverage, barrier safety and boundedness;
- selector-agnostic fixed-trace replay against K3/K3-safe and partition ceilings.

### `EXPERIMENT_BLOCKED`

Report the missing source/artifact or structural incompatibility. Do not invent a new representation or tune a grid.

