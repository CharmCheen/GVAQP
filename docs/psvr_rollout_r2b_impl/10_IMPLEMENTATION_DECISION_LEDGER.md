# Implementation decision ledger

The primary configuration is 64×64 and all other grid points are diagnostic.
The implementation does not expose a runner, seed API, file reader or experiment
result writer. Remaining execution authority is explicitly absent.
