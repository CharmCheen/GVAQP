# Model error operators

Error operators are planner-only. They implement A1 probability/duration bounds
and A2 canonical candidate/action/pair identities, keyed signs and empty-frontier
rules. They never mutate an evaluator environment.
