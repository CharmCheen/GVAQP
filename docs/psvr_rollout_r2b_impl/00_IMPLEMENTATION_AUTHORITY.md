# H1B implementation authority

This implementation consumes the recursively frozen BASE→A1→A2→A3 contract.
It adds only result-blind, visible-state planning components; it creates no H1B
seed, runner, episode, ledger, aggregate, or scientific outcome.
