# Component architecture

`types`, `budgets`, `crn`, `advantage`, `uncertainty`, `error_operators`,
`planning_cost`, `fallback`, and `approximate_rollout` form the approximate
visible-only path. `exact_reference` is a separately typed evaluator boundary.
