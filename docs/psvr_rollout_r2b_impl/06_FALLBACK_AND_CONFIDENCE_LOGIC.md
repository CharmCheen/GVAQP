# Fallback and confidence

Point fallback requires a strictly positive mean. LCB fallback requires a
strictly greater frozen LCB than bias plus planning overhead. Ties fall back to
pi0. No multiple-comparison correction is added because A1 fixed the normal
per-action LCB formula and named one primary configuration.
