# Exact reference boundary

ApproximatePlanner accepts only a visible base-policy callable and paired model
API. It cannot accept an ExactEvaluatorAPI. Exact reference records are
evaluator-only diagnostics and cannot enter planner traces.
