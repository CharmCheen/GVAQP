# V2 decision ledger

Audit found the frozen coverage registry has 13 rows while the IC1 runner appends six rows at runtime. V2 cannot decide whether 13-row or 19-row coverage is the same immutable development universe without changing/choosing a scientific universe.
