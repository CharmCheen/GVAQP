# Immutable universe contract

The coverage registry, seed registry and execution source must define one identical ordered identity universe. Any runtime extension absent from the frozen registry is a scientific-universe ambiguity and fails closed.
