# Primitive evidence contract

Each trace must provide canonical identity, immutable universe manifest, complete canonical visible history, history-completion ticks, replay seed/key, and each paired sample's root index/world, trajectory, canonical pre-action visible state, action/base identity and posterior selection key. Missing any field is `BLOCKED_LOSSLESS_EVIDENCE`.
