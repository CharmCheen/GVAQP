# V1 decision ledger

The first V1 layer is fail-closed primitive evidence admission. Current compact evidence has only visible-history hashes and untrusted model pre-states; it cannot support independent replay.
