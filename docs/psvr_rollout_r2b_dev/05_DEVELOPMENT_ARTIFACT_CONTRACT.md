# Artifact contract

Raw traces are source evidence and carry visible-only planner traces plus hashes.
Any debug derivative must carry `DEBUG_ONLY_NOT_FOR_SCIENTIFIC_USE` and
`NON_CONFIRMATORY` labels.
