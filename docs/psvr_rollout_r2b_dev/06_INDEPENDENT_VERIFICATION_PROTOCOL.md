# Independent verification

Verifier starts with ledger and raw trace hashes, validates exact coverage and
non-confirmatory labels, and rejects forbidden reference fields. It does not
read aggregate outputs as truth.
