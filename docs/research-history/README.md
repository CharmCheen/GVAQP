# Curated research history

These summaries preserve negative scientific results without migrating rejected implementations or raw traces. Every result is scoped to the frozen source evidence and is not promoted to a current algorithm.
