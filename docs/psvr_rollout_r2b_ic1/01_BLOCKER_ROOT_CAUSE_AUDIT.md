# Blocker root-cause audit

Observed pre-IC1 defects were: missing `error_form` and `error_magnitude` in the development identity; one-action execution followed by forced STOP; an exact-reference interface without realization; and verbose per-scalar raw evidence. These are implementation/evidence defects, not H1B scientific results.

IC1 preserves legacy repair assets as debug-only and never uses their metrics as confirmatory evidence.
