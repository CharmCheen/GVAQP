# Contaminated universe disposition

The old `TOY_HELDOUT_SEEDS.json` is retained unchanged under its recorded hash.
R2 loaders reject its absolute path, hash, and each seed identity. It is only
audit/regression/non-confirmatory debugging evidence, never R2 confirmation,
policy selection, parameter selection, Gate selection, or a primary result.
