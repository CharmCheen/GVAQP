# Curated results

This directory contains only the frozen current decisions, selected controller metadata, and key summary metrics needed to interpret them. It deliberately excludes action/frame traces, predictions, Parquet files, caches, search configurations, media, and weights. `manifests/artifact_manifest.json` hashes this directory excluding itself.
