# Closed, consumed, and deferred branches

Closed means no further investment on the current mechanism and evidence. It
is not a universal impossibility theorem.

| Branch | Status | Decisive reason | Reopen condition |
|---|---|---|---|
| Initial fixed-bisection DATB | **CLOSED** | T0 provenance does not support the claimed adaptive identity. | A fresh algorithm with a preregistered contract; do not rename this implementation. |
| Later value-rate DATB | **CLOSED** | Failed synthetic interaction gate and loses to the per-row ExSample envelope on all six T2 workloads. | No tuning reopen; a materially different mechanism starts at B0. |
| Current six T2 video-query workloads | **CONSUMED FOR SELECTION** | Repeatedly used for diagnosis and comparator choice. | Read-only diagnosis only, never confirmatory selection. |
| Per-row ExSample envelope | **DIAGNOSTIC UPPER COMPARATOR** | It selects a chunk per row with outcome knowledge; 73.1% of its absolute reported gap is oracle-selection bonus over the best global fixed chunk. | Use preregistered fixed or training-only selection on fresh held-out workloads. |
| Coverage-Debt and Dynamic V3 controllers | **NO-GO / CLOSED** | No stable held-out advantage; fixed scheduling is stronger on the cached substrate. | B0-B2 establish a new observable failure mode. |
| MAB/selective-deviation controller | **NO-GO / CLOSED** | Cached public-state learning does not generalize and fixed always-VERIFY has lower regret. | Prospective evidence after B0-B2. |
| Complex K3 novelty | **NO-GO / CLOSED** | Gap-only C1 explains nearly all measured gain. | External validity identifies a specific C1 failure and testable fix. |
| Existing model-relative P2 as escalation authority | **CLOSED** | It is inconclusive and non-authorizing. | Cannot reopen itself. |
| Human P1 | **DEFERRED, NOT CLOSED** | User does not currently want human audit. | Explicit future authorization; infer nothing from the empty label log. |

Retained bounded facts: C1 is a simple materializer; fixed SCAN1→VERIFY1 and
StaticProxyRank are cached/model-relative baselines, not universal solutions.
