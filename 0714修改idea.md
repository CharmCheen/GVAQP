# Deadline-Aware Progressive Video Event Retrieval
## 课题工作指导文档 v4（Evidence-Aligned Oracle-Defined Reference 版）

> **中文题目：面向时限的渐进式长视频事件检索**  
> **执行框架名称：PSVR**（Progressive Scan – Verify – Refine）  
> **文档类型：课题总纲，统一系统实现、实验设计与论文写作的工作基准**  
> **当前状态：Frozen Oracle Reference 已通过完整性审计；ARC-CACHED-REPLAY-v1 已完成并通过独立复核；PSVR 的跨视频核心方法信号尚未成立，physical ARC runner 仍未就绪。**
> **版本关系：本文档更新并取代《课题工作指导文档 v2（收敛版）》中关于 pseudo-reference、人工标注前置 Gate 和 Stage 0 优先级的定义，其余已冻结研究纪律继续保留。**

---

# 0. 文档目的与版本说明

上一版本已经将课题从过度扩张的“渐进式事件关系数据库理论”收敛为可执行的 PSVR 系统问题，但在 reference 定义上仍混合了两种不同目标：

1. **系统内部正确性**：在有限时间内，近似恢复由冻结语义 oracle 全量执行定义的精确答案；
2. **现实语义外部有效性**：该 oracle 的判断是否与人类对真实危险事件的判断一致。

本项目当前研究的是第一类问题。语义 oracle 是查询规范的一部分，其全量执行结果就是 exact answer，而不是等待人类校验后才成立的“伪真值”。因此，本版本做出以下修订：

| 类别 | 本版本处理方式 | 具体内容 |
|---|---|---|
| **核心修订** | 重新定义 reference | 将 dense-VLM pseudo-reference 改为 Frozen Oracle-Defined Reference |
| **Stage 0 修订** | 删除人工标注前置 Gate | Stage 0A 改为 Frozen Oracle Benchmark Integrity Audit |
| **资源修订** | 区分无 GPU 与有 GPU 工作 | 无 GPU 时完成 benchmark audit 与 functional replay；GPU 可用后完成 physical latency gate |
| **外部有效性** | 降级为可选分析 | Human annotation 不阻塞 Paper 1，只限制现实语义主张边界 |
| **原样保留** | 研究纪律不放松 | 独立视频统计协议、no-leakage、novelty boundary、related work、kill criteria、可复现性 |
| **低成本保留** | 保留系统差异点 | boundary honesty、verification state、最小 revision log |

沿用三类标注：

- **[事实]**：来自已有实现、冻结 benchmark 或实测结果；
- **[判断]**：基于已有事实做出的研究路线选择；
- **[待验证]**：必须通过新实验才能成立的主张。

## 0.1 当前证据边界（v4 新增，优先级高于历史计划）

本节是对后续章节的状态约束。若历史章节与本节冲突，以本节、
[`docs/PSVR_DECISION_LEDGER.md`](docs/PSVR_DECISION_LEDGER.md) 和
[`reports/ARC_BASELINE_READINESS.md`](reports/ARC_BASELINE_READINESS.md) 为准。

| 项目 | 当前状态 | 可以说什么 | 不能说什么 |
|---|---|---|---|
| Frozen oracle reference | **PASS**；347/347 raw reconstruction、26/26 events | oracle-defined benchmark 可用于内部一致性评价 | 不能把它称为 human-truth 或现实道路安全真值 |
| Runtime capability isolation | **PASS**；15 项隔离测试 | OracleAccessor / no-leakage 是硬约束 | 不能把 legacy runner 的历史结果自动视为合规 physical 证据 |
| ARC-CACHED-REPLAY-v1 | **完成**；180/180 runs、独立重算通过 | 可作为 causal cached replay 与 shared evaluator 的基线/诊断包 | 不能称为 native ARC reproduction 或 physical wall-clock 结果 |
| PSVR 两视频核心方法 | **NO_GO / NOT_YET**；无稳定跨视频质量信号 | 保留失败证据与问题定义，停止当前规则路线扩张 | 不能声称 Coverage-Debt、H-EXPOSE2 或 H-STAGE1 已验证 |
| Cold process + cold proxy | **NO_GO**；串行 T_min 大于 T_max | 该 regime 不适合作为 Paper 1 主场景 | 不能继续以 strict-cold partial-proxy 作为已成立动机 |
| Warm oracle + cold proxy | **GO（开发 workload）** | 可用于下一轮合规 physical integration | 不能外推到硬件、视频或部署环境 |
| Physical ARC | **未就绪** | 可以完善 runner、admission、snapshot 与输入绑定 | 不能提前运行或宣称 ARC physical smoke 已完成 |
| Held-out 研究支持 | **不足**；第三独立长源视频与 Cycle-1 oracle 支持仍缺 | 可以写成明确的 feasibility gate | 不能声称跨视频泛化、稳定收益或 SOTA |

因此，当前 idea 不是“继续寻找一个更复杂的 scheduler”，而是一个带有
明确证伪门槛的研究路线：先证明合规的 physical/cached execution contract
成立，再判断 progressive scheduling 是否值得成为论文贡献。当前最强的诚实
贡献类型是 **benchmark/protocol + systems diagnosis**；算法创新仍标为
`needs-evidence`，而不是默认成立。

## 0.2 标准化 Idea Card

**Target venue / family：** 未冻结；候选为 CV/Multimedia、DB/IR 或 Systems，
需根据最终证据包和 related-work search 再定。当前建议按跨 venue 可审计的
systems/protocol paper 设计，而不是按单一方法 paper 设计。

**Field：** deadline-aware long-video event retrieval，涉及 progressive
execution、semantic oracle access control、temporal event materialization。

**Raw idea：** 在 proxy 尚未完整物化、语义 oracle 调用昂贵且存在 hard
wall-clock deadline 时，渐进地扫描、验证并输出事件关系。

**Problem：** 现有实验协议容易把 oracle-call budget、cached proxy、完整
ranking 和 physical wall-clock 混在一起，导致方法排序无法解释；同时，
positive unit 可能被错误地报告成精确事件边界。

**Gap：** 需要一个 causal、leakage-safe、可 replay、能把 decode/proxy/oracle/
materialization/commit 统一计时的执行协议，并在此协议上检验 progressive
scan 是否在 equal-time 下带来可重复收益。

**Root challenge：** 未观测的 proxy 和未查询的 oracle label 都是不可见状态，
但系统又必须在中途提交一个可解释结果；任何“完整排序后再选择”的实现都会
破坏因果性或把 cache/replay 成本误报为 physical latency。

**Core insight（待验证）：** 只有当 proxy 计算本身跨越 deadline、且 oracle
verification 能在同一 deadline 内安全提交时，coverage-aware scan/verify
interleaving 才可能优于 scan-then-verify；否则它只是复杂度增加。

**Contribution type：** 第一优先是可审计的 execution protocol / benchmark
contract；第二优先才是一个简单、可证伪的 scheduling mechanism。不要同时
声称新的 oracle、新的事件语义、理论保证和 learned scheduler。

**Expected evidence：** frozen reference integrity、cached replay contract、
physical ARC integration、equal-time baseline、跨独立 source video 的
held-out 结果、组件归因和失败分析。

**Main risk：** 当前已有证据显示两视频 PSVR 方法路线没有稳定质量信号；若
补足输入并完成合规 physical integration 后仍无 equal-time gain，论文应收敛
为 protocol/negative-result/diagnostic paper，或停止该算法主线。

---

# 1. 核心问题（唯一收敛版本）

给定：

- 长视频 \(V\)；
- 事件查询 \(q\)（如“行人横穿”“车辆切入”）；
- 一组视觉 proxy \(\mathcal O_p\)；
- 一个冻结语义 oracle \(\mathcal O_o\)；
- hard wall-clock deadline \(T\)；
- 硬件与 serving 环境 \(H\)。

首先定义冻结 oracle 对视频单元全集 \(U(V)\) 的全量执行结果：

\[
Y^\star=\{\mathcal O_o(u,q)\mid u\in U(V)\}.
\]

再通过冻结、确定性的 reference materializer \(\mathcal M^\star\) 构造 exact EventRelation：

\[
R^\star_{\mathcal O}=\mathcal M^\star(Y^\star).
\]

系统在任意 \(t\le T\) 返回：

\[
S(t)=\{(s_i,e_i,y_i,\text{vstate}_i,\text{bstate}_i)\},
\]

目标是在不访问未查询 oracle label 的前提下，最大化：

\[
Q(S(T),R^\star_{\mathcal O})
\]

以及：

\[
\operatorname{AnytimeAUC}
=
\frac{1}{T}\int_0^T Q(S(t),R^\star_{\mathcal O})\,dt.
\]

**这是本课题唯一的核心问题：**

> 在真实 wall-clock deadline 下，渐进扫描视频、选择性调用冻结 oracle，并尽快恢复由 exhaustive oracle execution 定义的事件关系。

本项目不在 Paper 1 中研究 oracle 本身是否符合人类真实语义。人类标注只作为可选的 external-validity study。

---

# 2. Oracle-Defined Semantics 与 Exact Answer

## 2.1 Oracle 是查询规范的一部分

定义查询时，必须同时冻结：

```text
oracle model / endpoint
model version
prompt template
frame sampling policy
clip construction policy
inference parameters
retry policy
response parser
label normalization rule
failure handling rule
```

于是 oracle 不只是实现细节，而是查询语义的一部分：

\[
q_{\mathcal O}=(q,\mathcal O_o,\mathcal M^\star).
\]

不同 oracle、prompt 或 parser 对应不同查询定义，不应混成同一个 benchmark。

## 2.2 Frozen Oracle-Defined Reference

本文统一使用以下术语：

- **Frozen Oracle-Defined Reference**；
- **Dense Oracle Reference**；
- **Exhaustive Oracle Reference**。

不再将其称为 pseudo-ground-truth，除非专门讨论它与人类语义的差异。

如果 oracle 具有随机性，则冻结一次完整 raw-response realization：

\[
\mathcal O_o^{\mathrm{frozen}}.
\]

所有被测方法必须针对同一份冻结 realization 评价。

## 2.3 Exact answer 的构成

Exact answer 不只是 unit-level labels，而是：

```text
complete oracle outputs
+ frozen parser
+ frozen materializer
+ frozen boundary convention
= exact EventRelation
```

reference materializer 必须冻结：

```text
positive grouping rule
queried-negative / dense-negative barrier rule
gap rule
maximum duration
merge / split convention
boundary convention
tie-breaking rule
```

## 2.4 不构成循环评价

被测系统使用同一个 oracle，reference 也由该 oracle 全量运行生成，不构成评价循环。系统研究的是：

\[
\text{sparse / progressive oracle execution}
\approx
\text{exhaustive oracle execution}.
\]

对应 AQP 中的：

```text
exact full execution answer
vs.
approximate budgeted execution answer
```

评价不试图证明 oracle 是否符合人类道路安全判断。

---

# 3. Formal Problem Definition

## 3.1 输入

\[
\mathcal I=(V,q,\mathcal O_o,\mathcal M^\star,T,H).
\]

第一版不引入用户偏好权重，避免把尚未验证的多目标 utility 混入核心问题。

## 3.2 动作

```text
SCAN(region, level, proxy_config)
VERIFY(candidate, oracle_config)
REFINE_BOUNDARY(event_core, side, oracle_config)
```

`SCAN` 的 `level` 控制粒度，支持先粗后细的 temporal coverage。

每个动作有随机运行时间 \(C(a)\) 和输出 \(Z(a)\)。所有动作均受 deadline admission 约束：

\[
\widehat C_{p95}(a)
+
\widehat C_{p95}(\text{materialize+snapshot})
+
\epsilon
<
T-t.
\]

冷启动阶段使用离线 profiling 的 p95 先验；在线样本达到阈值后，才允许切换到在线估计。

第一版冻结：

```text
minimum_online_samples = 10
```

## 3.3 状态

\[
s_t=(\text{ObsLog}_t,\text{Candidates}_t,\text{Segments}_t,\Gamma_t,R_t,\widehat C_t).
\]

其中：

- `ObsLog`：append-only observation ledger；
- `Candidates`：仅由已扫描区域产生的候选；
- `Segments`：当前 proxy candidates、confirmed cores 与 boundary states；
- \(\Gamma_t\)：temporal coverage map；
- \(R_t\)：当前 materialized result；
- \(\widehat C_t\)：operator latency profile。

## 3.4 输出

在任意 \(t\le T\) 输出当前 `Segments(t)`。

```text
strict view      = verification_state == oracle_confirmed
exploratory view = verification_state in {proxy_candidate, oracle_confirmed}
```

`oracle_rejected` 可以保存在内部表和 negative barrier 中，但不能作为用户事件返回。

---

# 4. 输出 Schema

## 4.1 Segments 表

```text
segment_id
event_type
start_time
end_time

evidence_core_start
evidence_core_end

boundary_state:
  - core_only
  - left_refined
  - right_refined
  - fully_refined

verification_state:
  - proxy_candidate
  - oracle_confirmed
  - oracle_rejected

score
evidence_refs
last_updated_at
```

保留 `evidence_core_*` 与 `start_time/end_time` 的区分：命中一个 10 秒 unit 只能证明事件与该 unit 相交，不能证明整个 unit 都是事件。

## 4.2 Revision Log

```text
revision_id
segment_id
timestamp
field_changed
old_value
new_value
trigger_observation_id
```

这不是完整 EventRelation DAG，只用于：

1. 调试与 replay；
2. 统计 `silent_revision_count`；
3. 判断 Paper 2 是否需要完整版本化语义。

## 4.3 Observation Log

至少记录：

```text
observation_id
query_id
video_id
unit_or_region
operator
operator_version
input_config_hash
raw_output_ref
normalized_output
physical_or_replay
wall_start_ns
wall_end_ns
elapsed_ms
success
error_type
parent_action_id
```

`physical_or_replay` 必须显式区分：

```text
physical oracle invocation
cached oracle replay
logical oracle access
```

---

# 5. 系统架构：PSVR

系统由五个模块组成：

1. **Wall-Clock Runner**：记录 decode、proxy、oracle、K3、boundary refinement、queue、batch 和 snapshot 的真实耗时；
2. **Progressive Scan**：支持 sequential、uniform temporal、time-stratified、streaming-PSTR 和 coverage-first hierarchical；
3. **Oracle Interleaving**：第一版只使用规则式比例，不实现 learned scheduler；
4. **Online K3 / Event-Core Grouping**：每获得新 positive 或 negative evidence 后确定性 replay；
5. **Boundary Refinement + Snapshot Evaluator**：恢复 confirmed core 两侧边界，在固定 wall-clock checkpoint 输出结果。

流程：

```text
new observation
→ append ObsLog
→ update candidate list
→ query OracleAccessor if VERIFY
→ deterministic K3 replay
→ update boundary state
→ append revision log if changed
→ emit complete snapshot
```

第一版优先保证语义正确、无泄漏、可 replay，不提前优化增量 K3。

---

# 6. Cold / Warm / Replay Workload

## 6.1 Cold workload

计入：

```text
process/model initialization
video open
decode
proxy
oracle
queue waiting
K3
snapshot
```

完整 proxy 尚不存在。系统只能根据已扫描区域做决策。

## 6.2 Warm workload

proxy、embedding 或索引已经缓存。此时 partial-proxy 不再是主问题，主要研究 oracle scheduling 与 event materialization。

## 6.3 Functional replay workload

当 GPU 或 physical oracle 不可用时，允许使用 frozen oracle cache 完成：

```text
scheduler functional correctness
OracleAccessor access control
no-leakage tests
K3 replay
snapshot semantics
deadline-runner code path
call-budget / synthetic-cost trace validation
```

但 replay workload 不得用于声称：

```text
physical oracle latency
GPU proxy latency
cold-start latency
真实 partial-proxy wall-clock interval
硬件 portability
```

## 6.4 公平性

Cold workload 下：

- 不得读取未扫描区域的 proxy score；
- 不得直接打开 dense oracle cache；
- 只允许通过 `OracleAccessor.query(unit_id)` 访问已付费的逻辑 oracle label；
- Streaming-PSTR 只能对已观察 proxy scores 排序；
- 所有方法共享相同的 oracle、materializer、deadline accounting 和 snapshot checkpoints。

---

# 7. Stage 0A：Frozen Oracle Benchmark Integrity Audit

这是当前无 GPU 条件下的首要任务。

## 7.1 目标

确认 exhaustive oracle reference 是否满足：

```text
complete
frozen
traceable
deterministic
replayable
leakage-safe
```

## 7.2 必查项目

### Coverage

- 所有 benchmark units 是否都有 raw oracle response；
- 是否存在缺失 unit、重复 unit、错误时间戳或越界 clip；
- 每个 query 是否独立完整覆盖；
- positive / negative / parse-failure 数量是否可复现。

### Provenance

必须可追溯：

```text
benchmark id
source video hash
unitization config
model / endpoint
model version
prompt hash
frame sampling config
inference parameters
retry history
parser version
normalization rule
raw response path
```

### Deterministic reconstruction

从 raw responses 重新运行 parser 和 reference materializer，要求：

- unit labels 可确定性重建；
- reference events 可确定性重建；
- 重放结果与冻结 benchmark 一致；
- 若不能 bitwise 一致，必须解释可接受的语义一致性范围。

### No leakage

必须测试：

- scheduler 无法直接读取完整 cache；
- candidate ranking 无法访问未查询 labels；
- materializer 只能使用已查询 evidence；
- evaluation 模块与 execution 模块隔离；
- cache 文件路径不能通过普通 runtime config 暴露给 selector。

## 7.3 输出

```text
outputs/psvr_stage0a_oracle_audit/
├── FINAL_REPORT.md
├── benchmark_manifest.json
├── oracle_coverage.csv
├── provenance_audit.json
├── reconstructed_unit_labels.csv
├── reconstructed_reference_events.csv
├── replay_consistency.json
└── leakage_test_report.json
```

## 7.4 决策

```text
ORACLE_BENCHMARK_INTEGRITY = PASS
ORACLE_BENCHMARK_INTEGRITY = PASS_WITH_LIMITATIONS
ORACLE_BENCHMARK_INTEGRITY = BLOCKED
```

### PASS

- dense coverage 完整；
- provenance 足够；
- deterministic reconstruction 成功；
- no-leakage tests 通过。

### PASS_WITH_LIMITATIONS

- reference 可用，但存在非关键 provenance 缺失或历史配置无法完全重建；
- 必须在报告中限制主张并冻结当前 realization。

### BLOCKED

- 存在未覆盖 units；
- raw responses 无法追溯；
- reference 无法稳定重建；
- runtime 无法阻止未查询 label 泄漏。

Stage 0A BLOCKED 时，不得开展正式方法比较。

---

# 8. Stage 0B：Physical Wall-Clock / Partial-Proxy Regime Gate

Stage 0B 需要真实 GPU、proxy 和 physical oracle 环境后执行。

## 8.1 核心问题

\[
\exists T:\quad
\tau_{\text{coarse}}<T<\tau_{\text{full proxy}}
\]

且 coarse scan 后仍能完成至少一次：

```text
physical oracle verification
+ K3 materialization
+ complete snapshot commit
```

## 8.2 必测阶段

```text
cold initialization
video open
decode / seek
proxy preprocess
proxy inference
proxy postprocess
oracle queue
oracle preprocess
oracle inference
oracle postprocess
K3 replay
boundary refinement
snapshot serialization / commit
```

每类 operator 报告：

```text
p50 / p90 / p95 / mean / std / min / max
```

## 8.3 正式 interval

\[
C_{\text{commit}}
=
C_{p95}(\mathrm{K3})
+
C_{p95}(\mathrm{snapshot})
+
\epsilon.
\]

\[
C_{\text{verify-effective}}
=
C_{p95}(\mathrm{VERIFY})
+
C_{\text{commit}}.
\]

\[
T_{\min}
=
C_{p95}(\mathrm{coarse})
+
C_{\text{verify-effective}}.
\]

\[
T_{\max}=C_{p50}(\mathrm{full\ proxy}).
\]

报告：

```text
interval_width = T_max - T_min
normalized_interval_width = interval_width / tau_full_proxy
```

## 8.4 决策

```text
PARTIAL_PROXY_REGIME = GO
PARTIAL_PROXY_REGIME = WEAK
PARTIAL_PROXY_REGIME = NO_GO
```

不允许通过 sleep、模拟延迟或不现实降速制造 GO。

---

# 9. Stage 1：PSVR Functional Prototype

Stage 1 可以在无 GPU 条件下使用 cache replay 完成，目标是证明系统语义和执行路径正确，而不是证明真实 wall-clock 优势。

## 9.1 第一版动作

```text
SCAN
VERIFY
REFINE_BOUNDARY
```

## 9.2 第一版组件

```text
WallClockRunner
LatencyLedger
OracleAccessor
ProgressiveScanner
CandidateFrontier
OnlineK3Replay
SegmentsTable
RevisionLog
SnapshotEvaluator
DeadlineAdmissionGuard
```

## 9.3 第一版策略

只实现：

```text
Scan-Then-Verify
Coverage-Interleave
Sequential-Interleave
Oracle-Only-Stratified
```

Streaming-PSTR 可作为随后补充，但不得读取未来 proxy。

## 9.4 功能验收

必须验证：

1. 未查询 oracle labels 不可见；
2. 相同 action trace 可确定性 replay；
3. 每个 snapshot 都是完整提交结果；
4. deadline 停止后不启动无法安全提交的动作；
5. strict/exploratory 视图可由 verification state 过滤；
6. 单 positive unit 不被报告为精确边界；
7. negative evidence 可作为 barrier；
8. revision log 能记录字段变化；
9. execution 与 evaluation cache 物理隔离；
10. cache replay latency 不被误报为 physical latency。

决策：

```text
PSVR_FUNCTIONAL_REPLAY = PASS
PSVR_FUNCTIONAL_REPLAY = FAIL
```

---

# 10. Progressive Scan 与 Oracle Interleaving

## 10.1 Progressive Scan

需要支持以下扫描策略：

```text
sequential
uniform temporal
random temporal
time-stratified
streaming-PSTR
coverage-first hierarchical
```

第一版重点比较：

```text
sequential
uniform temporal
coverage-first hierarchical
```

Coverage-first 的最低要求：

- 短 deadline 下优先形成全局时间覆盖；
- 记录 proxy coverage；
- 记录最大连续未观测区间；
- 不只扫描视频前缀；
- 不提前读取完整 proxy ranking。

## 10.2 Candidate Frontier

候选只来自已经扫描的区域。至少记录：

```text
candidate_id
unit_id
temporal_cell
proxy_score
score_band
scan_level
verification_state
distance_to_confirmed_event
```

第一版可以使用 exploitation / audit 双队列：

```text
exploitation: 已观察区域中的高 proxy score 候选
audit: 中低分或覆盖不足区域中的代表候选
```

不实现 learned VOI。149/149 mechanism gate 已表明现有公共特征不足以支持复杂 utility scheduler。

## 10.3 规则式 interleaving

例如：

```text
coarse coverage 未完成：
  每扫描 N 个 region，验证 1 个当前最佳候选

coarse coverage 完成后：
  VERIFY 高优先候选
  否则 SCAN_REFINE 高 coverage-debt 区域
  再否则 REFINE_BOUNDARY
```

所有比例必须在开发集冻结后用于 held-out 实验。

---

# 11. Boundary Refinement 与 Boundary Honesty

## 11.1 基本语义

单个 10 秒 positive unit 只证明：

\[
E\cap U\neq\varnothing.
\]

不能证明：

\[
E=U.
\]

因此输出必须区分：

```text
evidence core
reported interval
boundary state
```

## 11.2 第一版 refinement

从 confirmed core 向左右邻接扩张：

```text
positive → 继续扩张
negative → 建立 barrier 并停止
deadline 不足 → 保持 core_only / 单侧 refined
```

可选的二级 refinement：仅在边界 10 秒 unit 内进一步切成 2 秒 clips。第一轮不做全视频帧级定位。

## 11.3 作用定位

Boundary refinement 是 Paper 1 的一个子机制，不应盖过核心问题。其主要价值是：

- 避免把 positive clip 当成完整事件；
- 提升 temporal IoU 和 boundary completeness；
- 让任意停止时刻的结果保持诚实。

---

# 12. Research Questions

## RQ0：Partial-proxy regime 是否真实存在？

\[
\exists T:\quad
\tau_{\text{coarse}}<T<\tau_{\text{full proxy}}
\]

且能完成有效 verification 与 commit。

RQ0 只能由 Stage 0B 的 physical profiling 回答。

## RQ1：调用次数评价与 wall-clock 评价是否得出不同结论？

同时报告：

```text
quality vs oracle calls
quality vs wall-clock time
```

验证完整 proxy overhead、decode、cold start、batch、queue 和 oracle latency 是否改变方法排序。

## RQ2：proxy 未跑完时，怎样扫描最有效？

比较：

```text
sequential
uniform temporal
random temporal
proxy-first on observed regions
time-stratified
coverage-first hierarchical
streaming-PSTR
```

重点回答 temporal coverage 是否带来可重复的 anytime 质量收益。

## RQ3：scan / verify / boundary-refine 如何分配时间？

比较：

```text
scan-only
scan-then-verify
scan-verify-interleave
scan-verify-boundary-interleave
```

观察：

```text
event recall
precision
F1
temporal IoU
boundary completeness
time-to-first-confirmed-event
AnytimeAUC
```

## RQ4（可选 external validity）：Oracle-defined events 与 human-defined events 的一致性如何？

RQ4 不阻塞 Paper 1。只有在需要扩大道路安全现实语义主张时才开展人工标注。

其结论只影响：

```text
oracle semantic scope
application validity
claim wording
```

不影响系统对 Frozen Oracle-Defined Reference 的内部正确性。

---

# 13. 实验设计

## 13.1 Baselines

| 方法 | Proxy 策略 | Oracle 策略 | Workload 注意事项 |
|---|---|---|---|
| Scan-Then-Verify | 先运行 progressive/full proxy | proxy 后统一验证 | cold 下计入全部 proxy 成本 |
| Coverage-Interleave | 多尺度全局覆盖 | 周期性交错 | 不读取未来 proxy |
| Sequential-Interleave | 时间顺序扫描 | 相同验证规则 | 作为简单 streaming baseline |
| Randomized-Coverage | 时间分层随机 | 相同验证规则 | 固定 seed 集 |
| Streaming-PSTR | 仅对已观察 proxy 排序 | 固定验证 | 禁止使用完整 ranking |
| Oracle-Only-Stratified | 不运行 proxy | 时间分层 oracle | 检验 proxy 是否值得 |
| Native ARC | ARC 原生调度 | ARC 原生 | 必须完整系统运行 |
| ARC + Shared K3 | ARC query trace | shared materializer | controlled comparison |
| Warm PSTR-5:4 | 完整 proxy 已缓存 | exact-B | 只属于 warm baseline |

PSTR-5:4 继续冻结为 warm workload 下的强 selector baseline，不再在当前阶段调参。

## 13.2 消融

分阶段执行：

1. coverage-first vs sequential / uniform；
2. scan-then-verify vs scan-verify-interleave；
3. boundary refinement on/off；
4. proxy-enabled vs oracle-only；
5. native materializer vs shared materializer。

不做第一轮全组合笛卡尔积。

## 13.3 Deadline Grid

统一 checkpoints：

```text
1, 2, 5, 10, 20, 30, 60, 120, 300, 600 seconds
```

同时报告：

```text
T_wall
T / tau_full_proxy
T / tau_dense_oracle
```

使用最近一个完整 snapshot 构造 piecewise-constant anytime curve。

## 13.4 指标

### 主要质量指标

```text
event precision
event recall
event F1
temporal IoU
boundary completeness
```

### Anytime 指标

```text
AnytimeAUC
time-to-first-confirmed-event
time-to-k-confirmed-events
quality at deadline
```

### 执行指标

```text
proxy coverage
max unobserved temporal gap
oracle calls
physical oracle calls
GPU-seconds
decoded frames
wall-clock breakdown
deadline miss rate
```

### 诊断指标

```text
silent_revision_count
overmerge
oversplit
event churn
cache-replay / physical mismatch
```

---

# 14. 统计协议

推断单位必须是：

\[
(\text{independent source video},\text{query}).
\]

不能把 units、events、deadlines 或同一视频切片当作独立样本。

采用：

```text
paired per-video comparison
cluster bootstrap
multi-query hierarchical bootstrap
per-video scatter
effect size + confidence interval
```

`AnytimeAUC` 为主终点，单 deadline 结果为辅助分析。

## 14.1 数据规模

正式 Paper 1 建议：

```text
N >= 12 independent source videos
>= 3 scene categories
>= 2 event queries
```

若资源不足，可以先做 pilot，但必须标记为 development evidence，不能声称跨视频稳定性。

## 14.2 Benchmark split

```text
dev videos:
  用于发现实现错误、冻结策略参数

held-out videos:
  reference 与配置冻结后一次性运行
```

不能在 held-out 结果上继续调 scan ratio、verify ratio、K3 或 deadline grid。

---

# 15. Kill Criteria

| 风险 | 触发条件 | 应对 |
|---|---|---|
| Oracle benchmark 不完整 | Stage 0A BLOCKED | 停止正式实验，修复 provenance / coverage / leakage |
| Partial-proxy regime 不存在 | RQ0 NO_GO | 转向 warm workload 下的 heterogeneous oracle scheduling |
| Coverage-first 无收益 | RQ2 无稳定效应 | 停止扩展扫描策略变体 |
| Interleaving 无收益 | RQ3 与 scan-then-verify 持平 | 不增加 scheduler 复杂度 |
| wall-clock 与 call-budget 结论相同 | RQ1 无排序差异 | 不把 deadline accounting 单独作为核心创新 |
| Boundary refinement 无实际价值 | IoU / completeness 无提升 | 保留 boundary honesty schema，删除复杂 refinement |
| 多独立视频不可复现 | 跨视频方差过大 | 停止调参，扩大 failure analysis |
| 收益来自更多 oracle 调用 | equal-time 不公平 | 修正实验，不接受该结果 |
| 系统只是 ARC/Zeus 参数变体 | Related work 无决定性差异 | 依赖 partial-proxy、真实 wall-clock、no-future-proxy 与 boundary honesty 的组合证据；若仍无法区分则重新定义贡献 |
| Human 与 oracle 不一致 | 可选 RQ4 显著失败 | 限制应用语义主张，不否定 oracle-defined benchmark 结果 |

NO_GO 是合法研究结论，不允许为了保留路线而修改问题定义。

---

# 16. Novelty 与 Related Work

## 16.1 不能声称

```text
首次使用 deadline
首次使用 proxy + oracle
首次 anytime 返回
首次处理 temporal events
首次做 boundary state
已经提供 human-truth recall guarantee
```

## 16.2 最小可防守 novelty

> Existing video query systems commonly evaluate budgets by oracle calls or assume that a complete proxy/index is already materialized. We study progressive event retrieval when proxy evidence itself is incomplete and all decode, proxy, oracle, materialization, and commit costs count toward a hard wall-clock deadline. The system incrementally scans, verifies, and refines event segments, while explicitly distinguishing confirmed evidence cores from unresolved boundaries at every stopping time.

中文：

> 现有视频查询系统通常按 oracle 调用次数评价预算，或默认完整 proxy / 索引已经物化。本文研究 proxy 证据本身尚未完整、并且 decode、proxy、oracle、事件物化和结果提交都计入 hard wall-clock deadline 的场景。系统渐进扫描、验证和恢复事件片段，并在任意停止时刻显式区分已确认的 evidence core 与尚未恢复的边界。

## 16.3 Oracle-defined semantics 的论文表述

> We treat the semantic oracle as part of the query specification. The exact answer is defined by exhaustive execution of the frozen oracle over all video units, followed by deterministic event materialization. Our goal is to approximate this oracle-defined EventRelation under a hard execution deadline.

## 16.4 必须对比

- **ARC**：完整系统 baseline，不只做 trace replay；
- **Zeus**：输入配置搜索 vs deadline 下的 progressive execution；
- **MIRIS**：单遍查询感知处理 vs 任意停止时刻的可提交事件结果；
- **SEIDEN**：proxy 成本不可忽略，与本文 partial-proxy 动机直接相关；
- **Online Aggregation**：标量估计 anytime semantics vs 离散事件集合恢复；
- **SUPG / ABae**：record selection 和 stratified allocation；
- **Allen interval algebra / CEP**：boundary state 与区间语义的理论背景。

---

# 17. 论文主张与结构（条件性版本）

## 17.0 论文路线选择

当前不得直接把本文档中的 PSVR 描述写成“已验证的方法”。论文路线按
以下顺序选择：

```text
protocol / benchmark contract
        ↓ physical ARC integration + independent-source support
equal-time method comparison
        ↓ stable held-out effect + component attribution
method paper claim
```

如果第二步没有稳定收益，保留 protocol、negative evidence 和 failure
analysis，删除“PSVR 提升 anytime retrieval”的强主张；如果 physical ARC
integration 本身无法完成，则只报告 cached replay，不能包装成系统论文的
真实 wall-clock 结果。

## 17.1 Paper 1 候选主张（尚未成立）

> We study deadline-aware progressive event retrieval over long videos. The exact answer is defined by exhaustive execution of a frozen semantic oracle and deterministic event materialization. Under a hard wall-clock deadline, a leakage-safe execution protocol progressively scans incomplete proxy evidence, selectively verifies candidates, and refines event boundaries. We evaluate whether a simple coverage-aware interleaving policy improves equal-time anytime retrieval over strong causal baselines; the claim is conditional on a valid physical regime and held-out cross-video evidence.

这段文字是研究假设，不是当前结果。只有同时满足以下条件才可升级为
论文 conclusion：

```text
ORACLE_REFERENCE_INTEGRITY = PASS
ARC physical contract = PASS
PARTIAL_PROXY_REGIME = GO or a predeclared warm/cold proxy GO regime
>= 3 independent source videos with frozen held-out evaluation
equal-time gain over scan-then-verify and ARC baseline
component attribution survives tie/coverage controls
```

否则摘要必须明确写 `cached replay`、`development evidence` 或
`protocol/diagnostic result`。

## 17.2 论文结构

1. Introduction
2. Background and Motivation
3. Oracle-Defined Problem Formulation
4. Frozen Benchmark and No-Leakage Execution Model
5. PSVR System Design
6. Experimental Methodology
7. Evaluation: RQ0–RQ3
8. Related Work
9. Limitations and External Validity
10. Conclusion

## 17.3 Reviewer-facing risk register

| 风险 | 类型 | 当前证据 | 触发条件 | 处理 |
|---|---|---|---|---|
| idea 只是已有 ARC/PSVR 的参数变体 | requires-new-evidence / likely-pivot | 当前两视频核心信号 absent | physical + held-out 仍无独立机制收益 | 收敛为 protocol/diagnostic paper 或停止算法主线 |
| cached replay 被误写成 physical result | writing-fixable，但高危 | ARC readiness audit 明确区分两者 | 任何结果表混用 workload | 每张表标注 cached/physical、cold/warm、oracle access |
| coverage gain 来自更多 oracle calls | design-fixable | 现有方法尚未完成合规 equal-time ARC physical comparison | call count 与 wall-clock 未成对报告 | equal-time、equal-call、oracle-only 三套对照 |
| 跨视频泛化不可判断 | evidence-fixable | 当前 `TWO_VIDEO_CORE_SIGNAL=ABSENT`，输入池也不足 | 无第三独立长源或 held-out 被调参 | 不扩张 claim；先完成 input/support gate |
| oracle-defined reference 被误解为真实语义 | writing-fixable | reference integrity 已 PASS，人类一致性未研究 | 论文使用 ground truth/recall guarantee | 全文使用 oracle-defined exact answer，并单列 external validity |
| 边界精度超过证据 | design-fixable | schema 已区分 evidence core/boundary state | positive unit 被直接转成精确 interval | 保留 boundary honesty 和 negative barrier 测试 |

---

# 18. 三阶段研究路线

## Paper 1：Deadline-Aware Progressive Event Retrieval

必须成立的主干：

```text
frozen oracle-defined benchmark
partial-proxy regime
coverage-aware scan
scan / verify interleaving
boundary honesty
wall-clock benchmark
```

这里的“必须成立”是进入 Paper 1 方法主张的条件，不是当前已完成清单。
当前已完成的是 oracle reference integrity 与 cached ARC replay；partial-
proxy method gain 尚未成立。

## Paper 2：条件触发

### 方向 A：Heterogeneous Operator Scheduling

进入条件：action features 在 held-out 视频上能稳定预测真实 \(\Delta Q/\text{second}\)。

### 方向 B：Versioned EventRelation

进入条件：`silent_revision_count`、merge/split 和用户可见矛盾在 Paper 1 中确实频繁且有害。

两个方向二选一，不同时启动。

在当前 `PSVR_TWO_VIDEO_SEARCH=NO_GO` 和 `PSVR_METHOD_USABILITY=NOT_YET`
的状态下，Paper 2 暂停，不得通过改名、调参或增加 learned/RL scheduler
绕过 kill criteria。

## Paper 3：Audited Event Recall

高风险方向，不阻塞 Paper 1。需要独立 identifiability study，不能提前声称 recall certificate。

---

# 19. 当前资源条件下的执行顺序（按证据重排）

当前顺序不再从“修改并冻结研究定义”开始；定义已在本文件和 oracle
contract 中冻结。近期顺序冻结为：

## Step 1：保留并复核已完成的 Stage 0A

当前证据为 `ORACLE_REFERENCE_INTEGRITY = PASS`。任何新 benchmark、prompt、
parser 或 materializer 变更都必须生成新 freeze identity，不能覆盖旧结果。

## Step 2：完成 ARC-CACHED-REPLAY 作为合规开发基线

当前已有 `ARC-CACHED-REPLAY-v1`：180/180 runs、共享 evaluator、causal
selection、artifact hash 和独立重算均已通过。它的用途是验证 selector/K3/
materialization 的逻辑和形成 baseline，不是 physical latency 证据。

## Step 3：完成 physical ARC contract，而非先扩展算法

必须先实现并测试：

```text
online complete proxy pass
fixed temporal clustering
selection-authorized OracleAccessor
unknown / timeout / parse-failure semantics
single monotonic deadline
tail-aware admission + durable snapshot
native candidate 与 confirmed EventRelation 分离
```

ARC readiness audit 当前明确为“未就绪”；在此之前不得运行 ARC physical
smoke，也不得把 legacy PSVR runner 重新命名为 ARC runner。

## Step 4：补足 independent-source support gate

先运行 label-independent input audit，确认第三独立长源、query support、
positive/negative coverage 和 source-session independence；获得明确 compute /
oracle authority 后，才执行 Cycle-1 extraction。未通过前不打开 paper
held-out semantics，不在开发集结果上调 scheduler。

## Step 5：执行最小 equal-time comparison

只比较预注册的少量路线：

```text
scan-then-verify
uniform/sequential interleave
coverage-aware candidate allocation
oracle-only stratified control
ARC cached replay / native ARC where available
```

同时报告 wall-clock、oracle calls、proxy coverage、quality、AnytimeAUC、
tail/admission failures 和 source-video clustered uncertainty。

## Step 6：依据 gate 选择论文形态

```text
physical GO + stable held-out gain      → method/system paper candidate
physical PASS but no gain               → protocol + negative/diagnostic paper
physical unavailable, cached pass only  → implementation/protocol note, no physical claim
support gate fails                      → stop algorithm expansion and document NO_GO
```

## 历史 Step：已完成的定义冻结

以下工作已完成，不再作为“下一步”重复执行：

- dense oracle reference 作为 exact answer；
- human reference 降级为可选 external-validity study；
- Stage 0A / Stage 0B / Stage 1 的基本验收条件；
- legacy 无效结果的隔离与强制 invalidation。

## 原 Step 2：Stage 0A 输出

目标输出：

```text
ORACLE_BENCHMARK_INTEGRITY = PASS / PASS_WITH_LIMITATIONS / BLOCKED
```

## 原 Step 3：PSVR Functional Replay（不要与 ARC-CACHED-REPLAY 混名）

若继续执行，应使用独立的 PSVR contract 和 frozen cache；当前 ARC cached
replay 的通过不能自动填充该 gate。使用 frozen cache，不声称 physical
latency。目标输出：

```text
PSVR_FUNCTIONAL_REPLAY = PASS / FAIL
```

## 原 Step 4：GPU 可用后完成 Stage 0B

目标输出：

```text
PARTIAL_PROXY_REGIME = GO / WEAK / NO_GO
```

## 原 Step 5：仅在 Stage 0A PASS 且 Stage 0B GO/WEAK 后开展正式 RQ1–RQ3

如果 Stage 0B NO_GO，则停止 cold partial-proxy 主线，转向 warm oracle scheduling。

---

# 20. 近期 Codex 任务边界

Codex 当前应完成：

```text
1. 维护 oracle/reference 的 hash-bound integrity 与 no-leakage contract
2. 完成 ARC physical runner 的 online proxy、admission、snapshot 绑定
3. 复核 ARC cached replay 与 native/physical 路径的同一 evaluator
4. 完成 independent-source input/support audit
5. 只运行预注册的 equal-time baseline 与 component attribution
6. 更新 decision ledger，明确 PASS / WEAK / NO_GO 和 claim scope
```

Codex 当前不应完成：

```text
1. 在 support gate 前继续新 selector 调参
2. learned VOI / bandit / RL scheduler
3. 没有 physical runner 时写完整 physical latency 结论
4. 把人工标注设为 Paper 1 前置 Gate
5. 大规模 baseline sweep 或打开 held-out semantics
6. 复杂 EventRelation DAG 或帧级 boundary model
7. 为得到 GO 人工制造延迟或用 sleep 伪造 regime
8. 将 cached replay、legacy trace 或 single-video pilot 改名为 native ARC
```

---

# 21. 当前验收清单

## 21.1 Stage 0A

```text
[x] exhaustive oracle coverage 完整（当前 frozen reference）
[x] raw responses 可追溯
[x] parser 与 materializer 冻结
[x] reference events 可确定性重建
[x] physical / replay / logical oracle 明确区分
[x] runtime 无未查询 label 泄漏
[x] benchmark manifest 完整
```

## 21.2 Stage 1

```text
[x] ARC cached replay 的 causal selector 只产生已观察区域候选
[x] ARC cached replay 的 VERIFY 只能通过授权 accessor
[x] shared K3/evaluator replay 确定性
[x] native candidates 与 confirmed EventRelation 分离
[x] replay artifact/hash 可复核
[x] replay latency 不冒充 physical latency
[ ] PSVR 独立 functional runner 的全部语义验收
[ ] ARC physical complete snapshot 与 monotonic deadline
[ ] ARC physical unknown/timeout/parse-failure 路径
```

## 21.3 Stage 0B

```text
[ ] GPU / physical ARC oracle 环境已准备
[ ] online complete proxy pass 已绑定并计费
[ ] operator latency 原始记录完整
[x] cold / warm 的定义和禁用范围已冻结
[ ] p95 admission guard 与 durable commit 已接入 ARC
[ ] T_min / T_max 可计算
[ ] regime decision 可追溯
```

---

# 22. 最终研究判断

当前课题核心保持不变：

> **在给定真实时限内，尽可能多、尽可能准确地恢复由冻结语义 oracle 定义的目标事件，并诚实表示尚未恢复的边界。**

本版本进一步明确：

1. exhaustive frozen oracle execution 是 exact answer；
2. 人工标注不是 Paper 1 的前置条件；
3. 当前无 GPU 时应先完成 benchmark integrity 与 functional replay；
4. partial-proxy wall-clock 主张必须等待 physical profiling；
5. cache replay 可以验证系统逻辑，但不能代替真实硬件证据；
6. 任何正式方法比较都必须通过 no-leakage OracleAccessor 执行；
7. 当前 PSVR 两视频方法路线已触发 NO_GO，不能继续以调参方式复活；
8. ARC-CACHED-REPLAY 是已完成的开发基线，不等于 native ARC 或 physical 结果；
9. 下一项最高信息量动作是完成 ARC physical contract 和 independent-source
   support gate，而不是增加 scheduler 复杂度。

当前最近的两个项目里程碑是：

```text
ORACLE_BENCHMARK_INTEGRITY = PASS
ARC_CACHED_REPLAY = PASS
```

下一阶段 gate 是：

```text
ARC_PHYSICAL_CONTRACT = PASS
INDEPENDENT_SOURCE_SUPPORT = PASS / BLOCKED
```

只有两者通过后，才重新评估：

```text
PARTIAL_PROXY_REGIME = GO / WEAK / NO_GO
PSVR_METHOD_USABILITY = VALIDATED / WEAK / NOT_YET
```

当前结论不是“PSVR 已经证明有效”，也不是“研究方向已经死亡”，而是：
问题定义和执行协议值得保留，算法主张必须等待合规 physical、独立来源和
equal-time 证据；在证据到达前，最稳妥的论文贡献是可审计 protocol、negative
evidence 和明确的失败边界。
