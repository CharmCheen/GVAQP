# Benchmark v2 Compatibility

This attempted v2 identity is not compatible/ready because strict historical oracle configuration equivalence is unresolved for 346 reused responses. Any semantic change requires a new benchmark ID (normally v3 unless this incomplete v2 attempt is rebuilt before acceptance). The immutable compatibility payload is ID-bound; the result envelope in `BENCHMARK_MANIFEST.json` is additively mutable.
