# Identity contradiction

Working claim: the historical responses belong to `realcartest_5k.mp4`.

Refined evidence: a session log says `realcartest_5k.mp4` is the first 5,000
frames of `realcartest.mp4`, so prefix lineage is supported. But the call runner
names the missing long parent, the video inventory gives it 3987.104 seconds,
and responses include intervals far beyond the 208.333333-second prefix. The
claim that all 399 responses belong to the available prefix bytes is rejected.
